local StatusLine = { bg = "NONE" }
local TabLine = { bg = "NONE" }
return {
  bg = StatusLine.bg,
  section_bg = StatusLine.bg,
  tabline_bg = TabLine.bg,
  buffer_bg = TabLine.bg,
  buffer_overflow_bg = TabLine.bg,
}
